- [ ] Diagnose why images are not showing (likely missing / wrong asset paths like `img/...` vs root files)
- [ ] Verify actual presence of referenced image files under `img/` folder
- [ ] Fix broken asset paths in `index.html` (and/or move/copy files if needed)
- [ ] Fix obvious JS/HTML bugs that can prevent correct rendering (e.g., `openvideo()` vs `openVideo()`, WhatsApp message uses `phone` undefined)
- [ ] Ensure page works in a browser after fixes

